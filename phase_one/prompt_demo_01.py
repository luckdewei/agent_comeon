import json

from llm_config import get_completion

"""
    原则1 写清晰、具体的指令
"""

# 战术 1：用分隔符隔离输入

# text = """
# 你应该用尽量简洁的方式表达你的想法。\
# 如果你不清楚自己的意思，你就无法通过文字让别人理解。\
# 不清晰的语言并不能显示你很聪明。
# """

# prompt = f"""
# 把三个反引号括起来的文本总结成一个句子。
# ```{text}```
# """

# response = get_completion(prompt)
# print(response)


# 战术 2：要求结构化输出（JSON / HTML）

# prompt = """
# 生成三本虚构的书名，包含作者和类型。
# 以下列 JSON 格式输出：
# {"books": [{"book_id": ..., "title": ..., "author": ..., "genre": ...}]}
# """

# response = get_completion(prompt)
# # print(response)
# print(json.loads(response))
# 输出合法 JSON，可直接用 json.loads() 解析

# 战术 3：让模型先验证条件

# text_1 = """
# 泡一杯茶很简单！首先，需要把水烧开。\
# 在等待的时候，拿一个杯子并把茶包放进去。\
# 一旦水足够热，就把它倒在茶包上。\
# 等待一会儿让茶浸泡。几分钟后，取出茶包。\
# 如果你喜欢，可以加入一些糖或牛奶调味。
# """

# text_2 = """
# 今天阳光明媚，鸟儿在\
# 唱歌。今天天气真好，适合出去走走 \
# 在公园里散步。花儿盛开，而且 \
# 树木在微风中轻轻摇曳。人们 \
# 外出活动，享受这美好的天气。 \
# 一些人在野餐，而另一些人在玩耍 \
# 玩游戏或在草地上放松。这是一个 \
# 户外休闲、享受美好时光的绝佳日子
# """

# prompt = f"""
# 你将获得由三个引号引起来的文本。\
# 如果它包含一系列的指令，则需要按照以下格式重新编写这些指令：\
# 第一步 - ...
# 第二步 - …
# …
# 第N步 - …

# 如果文本中不包含一系列的指令，则直接写"未提供步骤"。

# \"\"\"{text_2}\"\"\"
# """

# response = get_completion(prompt)
# print(response)
# 输出：
# 第一步 - 把水烧开。
# 第二步 - 拿一个杯子，把茶包放进去。
# 第三步 - 把热水倒在茶包上。
# ...


# 战术 4：Few-shot 示例提示

prompt = """
你的任务是以一致的风格回答问题。

<child>: 教我耐心。
<grandparent>: 挖出最深峡谷的河流源于一处不起眼的泉眼；\
最宏伟的交响乐从单一的音符开始；\
最复杂的挂毯以一根孤独的线开始编织。

<child>: 教我韧性。
"""
# 模型学习了祖父母说话的风格后，用类似的诗意语言回答

response = get_completion(prompt)
print(response)
